name: Playwright Tests
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
jobs:
  playwright-tests:
    name: 'Playwright Tests'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker Image with Cache
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile
          load: true
          tags: docker-playwright:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Start Containers And Run Tests
        working-directory: ./docker
        run: docker compose up --exit-code-from playwright-tests --abort-on-container-exit

      - name: Load Test Report History
        uses: actions/checkout@v6
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Deploy Allure Report To GitHub Pages
        if: always()
        run: |
          if [ -d "gh-pages-history/history" ]; then
            cp -r gh-pages-history/history allure-results/
          fi
          npx allure generate allure-results --clean -o allure-report

      - name: Deploy Report To Github Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history

      - name: Upload Allure Report Artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: allure-report
          path: ./reports/allure/

      - name: Tear Down Containers
        if: always()
        working-directory: ./docker
        run: docker compose down
